# Calxy

Calxy is a local-only React app for planning study or work focus sessions.

## Run locally

```sh
npm install
npm run dev
```

## Build locally

```sh
npm run build
npm run preview
```

The app uses browser `localStorage` for sessions and preferences. It does not need a backend, account, CDN, or external API at runtime.
